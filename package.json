{
  "name": "videowall",
  "version": "1.0.0",
  "description": "Video Wall Application",
  "scripts": {
    "start": "cd server && npm install && npm start",
    "build": "cd server && npm install",
    "dev": "cd server && npm run dev"
  },
  "engines": {
    "node": ">=18.0.0"
  }
} 